{
  "name": "cloudflare-cli",
  "version": "1.1.0",
  "description": "CLI for the cloudflare API",
  "type": "module",
  "bin": {
    "cloudflare-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/cloudflare-cli.js --target bun",
    "dev": "bun run src/index.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
